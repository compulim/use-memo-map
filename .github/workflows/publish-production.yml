name: Publish latest production

on:
  release:
    types: [released]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Download tarball
        run: gh release download ${{ github.ref }}
      - run: ls -la
  #     - run: npm publish --tag main `ls *.tgz`
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
